<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ellison.aop">

    <application>
        <activity android:name=".AopDetailActivity" />

        <activity android:name=".annotation.ExcutionAnnotationActivity" />
        <activity android:name=".method.ExecutionMethodActivity" />
    </application>

</manifest>
